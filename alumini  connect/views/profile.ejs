<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <style>
        /* General body styles */
        body {
            background: #121212;
            color: #ffffff; /* White text */
        }

        /* Profile container */
        .profile-container {
            max-width: 600px;
            margin: 50px auto;
            background: #1e1e1e; /* Dark gray background */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.8); /* Black shadow */
            text-align: center;
        }

        /* Profile image */
        .profile-image {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 20px;
            border: 3px solid #6c757d; /* Medium gray border */
        }

        /* Full name styling */
        .fullname {
            color: #f8f9fa; /* Off-white */
            font-size: 24px;
            margin-bottom: 10px;
        }

        /* Information labels and values */
        .info-label {
            font-weight: bold;
            color: #adb5bd; /* Light gray */
        }

        .info-value {
            color: #ffffff; /* White text */
        }

        /* Guest message styles */
        .guest-message {
            font-size: 18px;
            color: #adb5bd; /* Light gray */
        }

        /* Button styles */
        .btn-action {
            background-color: #6c757d; /* Medium gray */
            color: #ffffff; /* White text */
            border: none;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            display: inline-block;
            margin-top: 20px;
            transition: background-color 0.3s ease;
        }

        .btn-action:hover {
            background-color: #495057; /* Darker gray on hover */
        }

        /* Navbar and side menu adjustments */
        .navbar {
            background-color: #343a40;
        }

        .navbar .navbar-brand {
            color: #ffffff;
        }

        .navbar .navbar-brand:hover {
            color: #007bff;
        }

        .side-menu {
            background-color: #2d2d2d;
            height: 100vh;
            padding: 20px;
            color: #ffffff;
        }

        .side-menu a {
            color: #ffffff;
            text-decoration: none;
            display: block;
            padding: 10px;
        }

        .side-menu a:hover {
            background-color: #007bff;
            border-radius: 5px;
        }

        .content-wrapper {
            display: flex;
        }

        .content-wrapper .col-md-3 {
            width: 20%;
        }

        .content-wrapper .col-md-9 {
            width: 80%;
        }
    </style>
</head>
<body>
    <!-- Include the navigation partial -->
    <%- include('./partials/nav') %>

    <div class="container-fluid content-wrapper">
        <!-- Side Menu Section (Left Side) -->
        <div class="col-md-3 col-sm-4">
            <%- include('./partials/sidemenu') %>
        </div>

        <!-- Content Section (Right Side - Profile) -->
        <div class="col-md-9 col-sm-8">
            <div class="profile-container">
                <% if (user.role === 'USER') { %>
                    <img src="<%= user.profileImageUrl %>" alt="Profile Image" class="profile-image">
                    <h1 class="fullname"><%= user.fullname %></h1>
                    <p><span class="info-label">Email:</span> <span class="info-value"><%= user.email %></span></p>
                    <p><span class="info-label">Role:</span> <span class="info-value"><%= user.role %></span></p>
                <% } else { %>
                    <h1 class="guest-message">Hello, Guest!</h1>
                    <p class="guest-message">You are currently not logged in.</p>
                    <a href="/login" class="btn-action">Login</a>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
