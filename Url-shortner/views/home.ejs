<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1b1b1b; /* Navy Black background */
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background-color: #f44336; /* Aboli Red */
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.3);
            text-align: center;
            width: 400px;
        }
        h1 {
            color: #fff;
            margin-bottom: 20px;
        }
        input[type="text"] {
            width: 100%;
            padding: 12px;
            border-radius: 4px;
            border: none;
            margin-bottom: 20px;
            font-size: 16px;
            color: #333;
        }
        button {
            background-color: #333; /* Navy Black Button */
            color: #fff;
            border: none;
            padding: 12px 20px;
            font-size: 16px;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
        }
        button:hover {
            background-color: #555;
        }
        .result {
            margin-top: 20px;
            font-size: 18px;
        }
        .result a {
            color: #fff;
            text-decoration: none;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>URL Shortener</h1>
        <form action="/" method="POST">
            <input type="text" name="url" placeholder="Enter URL (e.g. http://google.com)" required>
            <button type="submit">Shorten URL</button>
        </form>

        <% if (shortUrl) { %>
            <div class="result">
                <p>Your shortened URL:</p>
                <a href="<%= shortUrl %>" target="_blank"><%= shortUrl %></a>
            </div>
        <% } %>
    </div>
</body>
</html>
